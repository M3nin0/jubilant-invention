//Aula sobre operadores lógicos


#include <stdio.h>

int main(void){

    //Operadores && (E, só retornam verdade, quando as duas condições em teste são verdadeiras)
    //Exemplo: if(2 > 1 && 4 > 2); printf("É maior\n");
    //Perceba que o printf será ativo, pois as duas condições são verdadeiras
    //Operadores || (OU, retorna verdade se uma das expressões em teste forem verdade)
    //Exemplo: if(2 > 4 || 3 > 1); printf("Exemplo 2\n");
    //Perceba que o printf será executado, já que pelo menos uma das expressões em teste é verdade


    return 0;
}
